# Required
NETHERMIND_CONFIG="{{ network }}"

NETHERMIND_JSONRPCCONFIG_JWTSECRETFILE="{{ jwt_secret_path }}"

# This should be set to only allow known hosts, allowing everyone for this example
NETHERMIND_JSONRPCCONFIG_HOST="0.0.0.0"